<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Expense Manager - Dashboard</title>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Feather Icons -->
  <script src="https://unpkg.com/feather-icons"></script>
  <!-- Chart.js -->
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

  <style>
    .try {
      font-size: 28px;
      color: #333;
      padding: 5px 0px;
    }
    .container {
      padding: 0px 20px 20px 20px;
    }
    .card.text-center {
      border: 3px solid #ccc;
      padding: 10px;
      margin: 10px;
      background-color: #f8f9fa;
      border-radius: 5px;
    }
    .card-title {
      font-size: 17.5px;
      margin-bottom: 1px;
      color: #333;
    }
    .card-text {
      font-size: 24px;
      font-weight: bold;
      color: #6c757d;
    }
    #sidebar {
      width: 250px;
      border-right: 1px solid #ccc;
      padding: 15px;
      background: #f5f5f5;
      height: 100vh;
    }
  </style>
</head>
<body>

<div class="d-flex">
  <!-- Sidebar -->
  <div id="sidebar">
    <div class="user text-center">
      <img class="img img-fluid rounded-circle" src="https://via.placeholder.com/120" alt="Profile">
      <h5>John Doe</h5>
      <p>johndoe@example.com</p>
    </div>
    <h6 class="mt-4">Management</h6>
    <ul class="list-unstyled">
      <li><a href="#"><i data-feather="home"></i> Dashboard</a></li>
      <li><a href="#"><i data-feather="plus-square"></i> Add Expenses</a></li>
      <li><a href="#"><i data-feather="dollar-sign"></i> Manage Expenses</a></li>
      <li><a href="#"><i data-feather="file-text"></i> Expense Report</a></li>
    </ul>
    <h6 class="mt-4">Settings</h6>
    <ul class="list-unstyled">
      <li><a href="#"><i data-feather="user"></i> Profile</a></li>
      <li><a href="#"><i data-feather="power"></i> Logout</a></li>
    </ul>
  </div>

  <!-- Main content -->
  <div class="flex-fill p-3">
    <h3 class="try">Dashboard</h3>
    <h4>Full-Expense Report</h4>

    <div class="row">
      <div class="col-md-3" id="card-today"></div>
      <div class="col-md-3" id="card-yesterday"></div>
      <div class="col-md-3" id="card-week"></div>
      <div class="col-md-3" id="card-month"></div>
      <div class="col-md-3" id="card-year"></div>
      <div class="col-md-3" id="card-total"></div>
    </div>

    <div class="row mt-4">
      <div class="col-md-6">
        <div class="card">
          <div class="card-header text-center">Daily Expenses</div>
          <div class="card-body">
            <canvas id="expense_line"></canvas>
          </div>
        </div>
      </div>
      <div class="col-md-6">
        <div class="card">
          <div class="card-header text-center">Expense Category</div>
          <div class="card-body">
            <canvas id="expense_category_pie"></canvas>
          </div>
        </div>
      </div>
      <div class="col-md-6 mt-4">
        <div class="card">
          <div class="card-header text-center">Monthly Expenses</div>
          <div class="card-body">
            <canvas id="monthly_expense_line"></canvas>
          </div>
        </div>
      </div>
      <div class="col-md-6 mt-4">
        <div class="card">
          <div class="card-header text-center">Yearly Expenses</div>
          <div class="card-body">
            <canvas id="expense_yearly_line"></canvas>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
feather.replace();

// SAMPLE DATA (Replace with API fetch calls later)
const today_expense = 500;
const yesterday_expense = 300;
const week_expense = 3500;
const month_expense = 15000;
const year_expense = 120000;
const total_expense = 500000;

const categories = ["Food", "Transport", "Shopping", "Bills"];
const category_expenses = [4000, 2000, 5000, 3000];

const daily_labels = ["Aug 4", "Aug 5", "Aug 6", "Aug 7", "Aug 8", "Aug 9", "Aug 10"];
const daily_data = [500, 700, 300, 900, 1200, 800, 600];

const monthly_labels = ["2024-09", "2024-10", "2024-11", "2024-12", "2025-01", "2025-02", "2025-03"];
const monthly_data = [12000, 15000, 11000, 17000, 20000, 18000, 19000];

const yearly_labels = ["2021", "2022", "2023", "2024", "2025"];
const yearly_data = [100000, 120000, 140000, 160000, 180000];

// FUNCTION to create expense cards
function createCard(title, amount) {
  return `
    <div class="card text-center">
      <div class="card-body">
        <h5 class="card-title">${title}</h5>
        <p class="card-text">₹${amount}</p>
      </div>
    </div>
  `;
}

// Insert cards
document.getElementById("card-today").innerHTML = createCard("Today's Expense", today_expense);
document.getElementById("card-yesterday").innerHTML = createCard("Yesterday's Expense", yesterday_expense);
document.getElementById("card-week").innerHTML = createCard("Last 7 Days", week_expense);
document.getElementById("card-month").innerHTML = createCard("Last 30 Days", month_expense);
document.getElementById("card-year").innerHTML = createCard("Current Year", year_expense);
document.getElementById("card-total").innerHTML = createCard("Total Expense", total_expense);

// Chart.js graphs
new Chart(document.getElementById('expense_category_pie'), {
  type: 'bar',
  data: {
    labels: categories,
    datasets: [{
      label: 'Expense by Category',
      data: category_expenses,
      backgroundColor: ['#6f42c1','#dc3545','#28a745','#007bff']
    }]
  }
});

new Chart(document.getElementById('expense_line'), {
  type: 'line',
  data: {
    labels: daily_labels,
    datasets: [{
      label: 'Daily Expenses',
      data: daily_data,
      borderColor: '#6f42c1',
      fill: false,
      borderWidth: 2
    }]
  }
});

new Chart(document.getElementById('monthly_expense_line'), {
  type: 'line',
  data: {
    labels: monthly_labels,
    datasets: [{
      label: 'Monthly Expenses',
      data: monthly_data,
      borderColor: '#fd7e14',
      fill: false,
      borderWidth: 2
    }]
  }
});

new Chart(document.getElementById('expense_yearly_line'), {
  type: 'bar',
  data: {
    labels: yearly_labels,
    datasets: [{
      label: 'Yearly Expenses',
      data: yearly_data,
      backgroundColor: '#28a745'
    }]
  }
});
</script>
</body>
</html>
